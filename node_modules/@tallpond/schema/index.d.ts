import { BucketBuilder, ResourceBuilder, TableBuilder } from './builders.js';
import type { SchemaIR } from './ir.js';
export * from './ir.js';
export { generateAgentTools, toolSourceFromIR } from './agentTools.js';
export type { GeneratedTool, JsonSchema, ToolBinding, ToolSource, ToolSourceResource, ToolSourceTable, } from './agentTools.js';
export type { InferRow, InferSchema } from './types.js';
export interface SchemaInput {
    tables?: Record<string, (t: TableBuilder) => void>;
    buckets?: Record<string, (b: BucketBuilder) => void>;
    resources?: Record<string, (r: ResourceBuilder) => void>;
}
export declare function defineSchema(input: SchemaInput): SchemaIR;
export interface MigrationDb {
    sql(strings: TemplateStringsArray, ...values: unknown[]): Promise<unknown>;
}
export interface Migration {
    run: (db: MigrationDb) => Promise<void>;
}
export declare function migration(run: (db: MigrationDb) => Promise<void>): Migration;
