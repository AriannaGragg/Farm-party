// TS row-type inference from the schema IR. Because the DSL uses mutating
// callbacks (table) => { table.text(...) }, precise per-column inference at the
// call site is limited; these helpers give the SDK a structured row type keyed
// by column name, refined further when the schema is registered (see the SDK's
// Register interface). Phase 1 uses these for shape, not exhaustive nullability.
export {};
